name: RDP

on:
  workflow_dispatch: 

jobs:
  rdp-job:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 1. إعداد Python 3.11
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 2. هندسة النظام: دعم العربية + ربط ملفات Python (الإصلاح النهائي)
      - name: Quick System Configuration
        shell: powershell
        run: |
          # تفعيل دعم اللغة العربية للكيبورد والنصوص (بدون تحميل حزم ثقيلة)
          $LangList = New-WinUserLanguageList -Language "en-US"
          $LangList.Add("ar-SA")
          Set-WinUserLanguageList -LanguageList $LangList -Force
          
          # استدعاء CMD لتنفيذ أوامر ارتباط الملفات (حل مشكلة الخطأ)
          $pythonPath = (Get-Command python).Source
          cmd /c "assoc .py=Python.File"
          cmd /c "ftype Python.File=`"$pythonPath`" `"%1`" %*"
          
          # إضافة امتداد .PY للامتدادات القابلة للتنفيذ
          [Environment]::SetEnvironmentVariable("PATHEXT", [Environment]::GetEnvironmentVariable("PATHEXT", "Machine") + ";.PY", "Machine")
          
          # تحسين أداء الشبكة
          netsh int tcp set global autotuninglevel=normal
          Write-Host "Symphony Mode: Python associated & Arabic enabled successfully!" -ForegroundColor Green

      # 3. تفعيل RDP وضبط النظام
      - name: Configure RDP
        shell: powershell
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
          Restart-Service -Name TermService -Force

      # 4. إنشاء المستخدم (RDP)
      - name: Create RDP User
        shell: powershell
        run: |
          $password = -join ((33..126) | Get-Random -Count 16 | % {[char]$_})
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          echo "RDP_USER=RDP" >> $env:GITHUB_ENV
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      # 5. تثبيت الأدوات مع التمويه الذكي (User-Agent Stealth)
      - name: Install Tools & Stealth Setup
        shell: powershell
        run: |
          choco install ffmpeg yt-dlp googlechrome aria2 -y
          python -m pip install --upgrade pip
          pip install yt-dlp aiohttp cloudscraper tqdm fake-useragent requests
          
          # إعداد التمويه لخداع يوتيوب
          $ua = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36"
          $configPath = "$env:USERPROFILE\yt-dlp.conf"
          @(" --user-agent `"$ua`"",
            " --referer `"https://www.google.com/`"",
            " --add-header `"Accept-Language: en-US,en;q=0.9,ar-SA;q=0.8`"",
            " --geo-bypass",
            " --external-downloader aria2c",
            " --external-downloader-args `"aria2c:-x 16 -s 16 -k 1M`""
          ) | Out-File -FilePath $configPath -Encoding utf8

      # 6. ربط Tailscale
      - name: Tailscale Connect
        shell: powershell
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $out = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $url -OutFile $out
          Start-Process msiexec.exe -ArgumentList "/i $out /quiet /norestart" -Wait
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner --accept-routes
          $tsIp = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "TS_IP=$tsIp" >> $env:GITHUB_ENV

      # 7. لوحة البيانات النهائية
      - name: Connection Info
        shell: powershell
        run: |
          Write-Host "`n===============================================" -ForegroundColor Green
          Write-Host "🚀 RDP SYSTEM READY (Optimized & Fixed)" -ForegroundColor Cyan
          Write-Host "==============================================="
          Write-Host "Tailscale IP : $env:TS_IP" -ForegroundColor Yellow
          Write-Host "Username     : $env:RDP_USER"
          Write-Host "Password     : $env:RDP_PASS" -ForegroundColor Red
          Write-Host "==============================================="

      # 8. الحفاظ على الاتصال
      - name: Keep Alive
        shell: powershell
        run: |
          while ($true) {
            Write-Host "[$(Get-Date)] RDP Server is active..."
            [System.GC]::Collect()
            Start-Sleep -Seconds 300
          }
